# Test Credentials

This application has **no authentication** in the current phase — all endpoints
are open. No login credentials are required to use the app or run tests.

- Frontend: https://standard-suggest.preview.emergentagent.com
- Backend API base: `${VITE_BACKEND_URL}/api`
- Database: PostgreSQL (see `backend/.env` `DATABASE_URL`), seeded with ~20 standards.
