# RAG layer (future)

Inert in the current phase. Will provide grounded, citation-backed reasons by
retrieving `chunks` from ingested spec `documents` and generating explanations.
`evidence` in the recommendation response is reserved for these citations.
