# Scripts

Operational scripts. The database seed lives at
`backend/scripts/seed_db.py` and is run with `python -m scripts.seed_db` from the
`backend/` directory (loads ~20 mock standards and their relationships).
